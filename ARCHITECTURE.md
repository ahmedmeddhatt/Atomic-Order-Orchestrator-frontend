# Architecture Documentation

## System Architecture Overview

This document provides detailed architectural diagrams and explanations for the Atomic Order Orchestrator system.

---

## 🏗️ High-Level Architecture

```
┌─────────────────────────────────────────────────────────────────────┐
│                          EXTERNAL SYSTEMS                           │
│                                                                     │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │                    SHOPIFY PLATFORM                          │  │
│  │  - Order Management                                          │  │
│  │  - Webhook Events                                            │  │
│  │  - Order Status Updates                                      │  │
│  └────────────────────────┬─────────────────────────────────────┘  │
└───────────────────────────┼─────────────────────────────────────────┘
                            │
                            │ HTTPS Webhooks
                            │ (orders/create, orders/update)
                            ▼
┌─────────────────────────────────────────────────────────────────────┐
│                      APPLICATION LAYER                              │
│                                                                     │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │                   BACKEND API (NestJS)                       │  │
│  │                      Port: 9000                              │  │
│  │                                                              │  │
│  │  ┌────────────────┐  ┌────────────────┐  ┌───────────────┐ │  │
│  │  │   Orders       │  │   Sync         │  │   Webhooks    │ │  │
│  │  │   Controller   │  │   Gateway      │  │   Controller  │ │  │
│  │  └────────┬───────┘  └────────┬───────┘  └───────┬───────┘ │  │
│  │           │                   │                   │         │  │
│  │  ┌────────▼───────────────────▼───────────────────▼───────┐ │  │
│  │  │              Orders Service                            │ │  │
│  │  │  - Business Logic                                      │ │  │
│  │  │  - Shipping Calculation                                │ │  │
│  │  │  - Status Mapping                                      │ │  │
│  │  └────────┬───────────────────────────────────────────────┘ │  │
│  │           │                                                 │  │
│  │  ┌────────▼───────────────────────────────────────────────┐ │  │
│  │  │              BullMQ Queue Manager                      │ │  │
│  │  │  - Job: process-order                                  │ │  │
│  │  │  - Retry Logic: 3 attempts                             │ │  │
│  │  │  - Concurrency: 5 workers                              │ │  │
│  │  └────────┬───────────────────────────────────────────────┘ │  │
│  │           │                                                 │  │
│  │  ┌────────▼───────────────────────────────────────────────┐ │  │
│  │  │              Orders Processor                          │ │  │
│  │  │  - Database Operations                                 │ │  │
│  │  │  - WebSocket Emission                                  │ │  │
│  │  │  - Error Handling                                      │ │  │
│  │  └────────────────────────────────────────────────────────┘ │  │
│  └──────────────────────────────────────────────────────────────┘  │
│                                                                     │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │                FRONTEND APP (Next.js)                        │  │
│  │                    Port: 3000/5000                           │  │
│  │                                                              │  │
│  │  ┌────────────────┐  ┌────────────────┐  ┌───────────────┐ │  │
│  │  │   Dashboard    │  │   Orders       │  │   Edit Order  │ │  │
│  │  │   Page         │  │   Table        │  │   Modal       │ │  │
│  │  └────────┬───────┘  └────────┬───────┘  └───────┬───────┘ │  │
│  │           │                   │                   │         │  │
│  │  ┌────────▼───────────────────▼───────────────────▼───────┐ │  │
│  │  │              React Query Cache                         │ │  │
│  │  │  - Query Key: ['orders']                               │ │  │
│  │  │  - Stale Time: 5 minutes                               │ │  │
│  │  │  - Cache Time: 10 minutes                              │ │  │
│  │  └────────┬───────────────────┬───────────────────────────┘ │  │
│  │           │                   │                             │  │
│  │  ┌────────▼───────┐  ┌────────▼───────┐                    │  │
│  │  │   useOrders    │  │ useOrderSync   │                    │  │
│  │  │   Hook         │  │ Hook           │                    │  │
│  │  └────────────────┘  └────────────────┘                    │  │
│  └──────────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────┘
                            │                   │
                            │ HTTP REST         │ WebSocket
                            ▼                   ▼
┌─────────────────────────────────────────────────────────────────────┐
│                      DATA LAYER                                     │
│                                                                     │
│  ┌──────────────────────┐  ┌──────────────────────┐               │
│  │   PostgreSQL         │  │   Redis              │               │
│  │   Port: 5432         │  │   Port: 6379         │               │
│  │                      │  │                      │               │
│  │  - Orders Table      │  │  - BullMQ Queues     │               │
│  │  - Indexes           │  │  - Job Data          │               │
│  │  - Constraints       │  │  - Cache (optional)  │               │
│  └──────────────────────┘  └──────────────────────┘               │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Data Flow Diagrams

### 1. Order Creation Flow

```
┌──────────┐
│ Shopify  │
└────┬─────┘
     │ 1. POST /webhooks/shopify
     │    (order data)
     ▼
┌─────────────────┐
│ Orders          │
│ Controller      │
└────┬────────────┘
     │ 2. Validate webhook
     │    Extract order data
     ▼
┌─────────────────┐
│ Orders          │
│ Service         │
└────┬────────────┘
     │ 3. Calculate shipping fee
     │    Map status
     │    Create DTO
     ▼
┌─────────────────┐
│ BullMQ          │
│ Queue           │
└────┬────────────┘
     │ 4. Add job: process-order
     │    Job ID: <uuid>
     ▼
┌─────────────────┐
│ Orders          │
│ Processor       │
└────┬────────────┘
     │ 5. Process job
     │    Save to database
     ▼
┌─────────────────┐
│ PostgreSQL      │
│ Database        │
└────┬────────────┘
     │ 6. Return saved order
     │    with version = 1
     ▼
┌─────────────────┐
│ Sync            │
│ Gateway         │
└────┬────────────┘
     │ 7. Emit ORDER_SYNCED
     │    to /sync namespace
     ▼
┌─────────────────┐
│ Frontend        │
│ (All Clients)   │
└────┬────────────┘
     │ 8. Update React Query cache
     │    Add order to list
     ▼
┌─────────────────┐
│ UI              │
│ Re-renders      │
└─────────────────┘
```

### 2. Real-Time Update Flow

```
┌──────────────┐
│ User A       │
│ (Browser 1)  │
└──────┬───────┘
       │ 1. Edit order
       │    Change status
       ▼
┌──────────────┐
│ Edit Modal   │
└──────┬───────┘
       │ 2. Submit form
       │    PUT /orders/:id
       ▼
┌──────────────┐
│ Backend API  │
└──────┬───────┘
       │ 3. Update database
       │    version++
       ▼
┌──────────────┐
│ PostgreSQL   │
└──────┬───────┘
       │ 4. Return updated order
       │    version = 2
       ▼
┌──────────────┐
│ Sync Gateway │
└──────┬───────┘
       │ 5. Emit ORDER_SYNCED
       │    to all clients
       ▼
┌──────────────────────────────┐
│                              │
▼                              ▼
┌──────────────┐    ┌──────────────┐
│ User A       │    │ User B       │
│ (Browser 1)  │    │ (Browser 2)  │
└──────┬───────┘    └──────┬───────┘
       │                   │
       │ 6a. Update cache  │ 6b. Update cache
       │    (optimistic)   │    (real-time)
       ▼                   ▼
┌──────────────┐    ┌──────────────┐
│ UI Updates   │    │ UI Updates   │
│ Immediately  │    │ Immediately  │
└──────────────┘    └──────────────┘
```

### 3. Conflict Resolution Flow

```
┌──────────────┐
│ User A       │
│ Opens Modal  │
└──────┬───────┘
       │ 1. Load order
       │    version = 1
       ▼
┌──────────────┐
│ Edit Modal   │
│ isDirty=false│
└──────┬───────┘
       │ 2. User makes changes
       │    isDirty = true
       ▼
┌──────────────┐
│ Edit Modal   │
│ isDirty=true │
└──────┬───────┘
       │
       │ Meanwhile...
       │
       ▼
┌──────────────┐
│ Server       │
│ Updates Order│
└──────┬───────┘
       │ 3. Emit ORDER_SYNCED
       │    version = 2
       ▼
┌──────────────┐
│ Edit Modal   │
│ Listener     │
└──────┬───────┘
       │ 4. Check conditions:
       │    - Same order? ✓
       │    - Newer version? ✓ (2 > 1)
       │    - isDirty? ✓
       ▼
┌──────────────┐
│ Conflict     │
│ Detected!    │
└──────┬───────┘
       │ 5. Show conflict alert
       │    with options
       ▼
┌──────────────────────────────┐
│ Conflict Resolution UI       │
│                              │
│ Your Change  │  Server Value │
│ SHIPPED      │  CONFIRMED    │
│                              │
│ [Refresh]  [Force Overwrite] │
└──────┬───────────────┬───────┘
       │               │
       │ 6a. Refresh   │ 6b. Force
       ▼               ▼
┌──────────────┐  ┌──────────────┐
│ Accept       │  │ Keep User    │
│ Server Data  │  │ Changes      │
│ version = 2  │  │ version = 2  │
└──────────────┘  └──────────────┘
```

---

## 🗄️ Database Schema

### Orders Table

```sql
CREATE TABLE orders (
  -- Primary Key
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  
  -- Shopify Integration
  shopifyOrderId VARCHAR(255) NOT NULL UNIQUE,
  lastExternalUpdatedAt TIMESTAMP NOT NULL,
  
  -- Order Data
  status VARCHAR(50) NOT NULL CHECK (status IN ('PENDING', 'CONFIRMED', 'SHIPPED', 'CANCELLED')),
  shippingFee DECIMAL(10, 2) NOT NULL CHECK (shippingFee >= 0),
  
  -- Optimistic Locking
  version INTEGER NOT NULL DEFAULT 1,
  
  -- Timestamps
  createdAt TIMESTAMP NOT NULL DEFAULT NOW(),
  updatedAt TIMESTAMP NOT NULL DEFAULT NOW()
);

-- Indexes for Performance
CREATE INDEX idx_orders_shopify_id ON orders(shopifyOrderId);
CREATE INDEX idx_orders_status ON orders(status);
CREATE INDEX idx_orders_updated_at ON orders(updatedAt DESC);
CREATE INDEX idx_orders_created_at ON orders(createdAt DESC);

-- Trigger for updatedAt
CREATE OR REPLACE FUNCTION update_updated_at_column()
RETURNS TRIGGER AS $$
BEGIN
  NEW.updatedAt = NOW();
  RETURN NEW;
END;
$$ LANGUAGE plpgsql;

CREATE TRIGGER update_orders_updated_at
BEFORE UPDATE ON orders
FOR EACH ROW
EXECUTE FUNCTION update_updated_at_column();
```

---

## 🔌 WebSocket Architecture

### Socket.IO Namespace Structure

```
Server (http://localhost:9000)
│
└── /sync (namespace)
    │
    ├── Connection Events
    │   ├── connect
    │   ├── disconnect
    │   └── error
    │
    └── Custom Events
        └── ORDER_SYNCED (server → client)
            ├── Payload: OrderSyncedDto
            ├── Room: 'sync' (broadcast)
            └── Trigger: Order created/updated
```

### Event Flow

```
┌─────────────────────────────────────────────────────────────┐
│                    SERVER SIDE                              │
│                                                             │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  Orders Processor                                    │  │
│  │  - Order saved to database                           │  │
│  │  - Inject SyncGateway                                │  │
│  └────────────────────┬─────────────────────────────────┘  │
│                       │                                     │
│                       ▼                                     │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  Sync Gateway                                        │  │
│  │  this.server.to('sync').emit('ORDER_SYNCED', data)   │  │
│  └────────────────────┬─────────────────────────────────┘  │
└───────────────────────┼─────────────────────────────────────┘
                        │
                        │ WebSocket Broadcast
                        │
┌───────────────────────┼─────────────────────────────────────┐
│                       ▼                                     │
│                    CLIENT SIDE                              │
│                                                             │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  useSocket Hook                                      │  │
│  │  - Connects to /sync                                 │  │
│  │  - Returns { on, emit, isConnected }                 │  │
│  └────────────────────┬─────────────────────────────────┘  │
│                       │                                     │
│                       ▼                                     │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  useOrderSync Hook                                   │  │
│  │  - Listens: ORDER_SYNCED                             │  │
│  │  - Updates React Query cache                         │  │
│  └────────────────────┬─────────────────────────────────┘  │
│                       │                                     │
│                       ▼                                     │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  React Query Cache                                   │  │
│  │  - queryClient.setQueryData(['orders'], ...)         │  │
│  │  - Surgical update (only changed order)              │  │
│  └────────────────────┬─────────────────────────────────┘  │
│                       │                                     │
│                       ▼                                     │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  UI Components                                       │  │
│  │  - Auto re-render                                    │  │
│  │  - No manual refresh needed                          │  │
│  └──────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
```

---

## 🎯 Component Architecture (Frontend)

### Component Hierarchy

```
App
│
├── RootLayout
│   ├── QueryProvider (React Query)
│   └── children
│
└── Dashboard Page
    │
    ├── Header
    │   ├── Logo
    │   └── Socket Status Badge
    │       └── isConnected (from useOrderSync)
    │
    ├── KPI Cards
    │   ├── Total Orders
    │   ├── Active Shipments
    │   ├── Revenue (mock)
    │   └── Avg Delivery Time (mock)
    │
    └── Orders Section
        │
        ├── Orders Table (Virtualized)
        │   ├── useOrders (data)
        │   ├── useVirtualizer (rendering)
        │   └── Rows
        │       ├── Order ID
        │       ├── Customer Info
        │       ├── Status Badge
        │       ├── Amount
        │       ├── Tracking Number
        │       └── Edit Button
        │
        └── Edit Order Modal
            ├── Form Fields
            │   ├── Status Dropdown
            │   ├── Tracking Input
            │   └── Notes Textarea
            │
            ├── Conflict Alert (conditional)
            │   ├── Your Change
            │   ├── Server Value
            │   ├── Refresh Button
            │   └── Force Overwrite Button
            │
            └── Footer
                ├── Version Display
                ├── Dirty Indicator
                ├── Cancel Button
                └── Save Button
```

### Hook Dependencies

```
useOrders
├── useQuery (React Query)
├── Fetches: GET /orders
└── Returns: { orders, isLoading, isError, updateOrder }

useOrderSync
├── useSocket
│   ├── io (Socket.IO Client)
│   └── Returns: { on, emit, isConnected }
├── useQueryClient (React Query)
├── Listens: ORDER_SYNCED
└── Returns: { isConnected }

useSocket
├── useEffect (connection lifecycle)
├── useRef (socket instance)
├── useState (connection status)
└── Returns: { socket, isConnected, on, emit }
```

---

## 🔐 Security Architecture

### Authentication Flow (Future)

```
┌──────────┐
│ User     │
└────┬─────┘
     │ 1. Login
     ▼
┌──────────┐
│ Auth     │
│ Service  │
└────┬─────┘
     │ 2. Verify credentials
     ▼
┌──────────┐
│ JWT      │
│ Token    │
└────┬─────┘
     │ 3. Return token
     ▼
┌──────────┐
│ Client   │
│ Storage  │
└────┬─────┘
     │ 4. Include in requests
     │    Authorization: Bearer <token>
     ▼
┌──────────┐
│ Backend  │
│ Guards   │
└────┬─────┘
     │ 5. Validate token
     ▼
┌──────────┐
│ Protected│
│ Resource │
└──────────┘
```

### Current Security Measures

1. **Environment Variables**: Sensitive data not in code
2. **CORS**: Configured allowed origins
3. **TypeORM**: SQL injection prevention
4. **React**: XSS prevention (auto-escaping)
5. **Webhook Validation**: Signature verification (optional)

---

## 📊 Performance Optimizations

### Frontend Optimizations

```
┌─────────────────────────────────────────────────────────┐
│ Performance Strategy                                    │
│                                                         │
│ 1. Virtualization                                       │
│    - Only render visible rows                           │
│    - 5,000+ orders → ~20 DOM nodes                      │
│    - 60 FPS scrolling                                   │
│                                                         │
│ 2. React Query Caching                                  │
│    - Deduplicate requests                               │
│    - Background refetching                              │
│    - Stale-while-revalidate                             │
│                                                         │
│ 3. Surgical Updates                                     │
│    - Update only changed order                          │
│    - No full list re-render                             │
│    - Preserve scroll position                           │
│                                                         │
│ 4. Code Splitting                                       │
│    - Next.js automatic splitting                        │
│    - Lazy load modals                                   │
│    - Route-based chunks                                 │
│                                                         │
│ 5. Memoization                                          │
│    - useMemo for expensive calculations                 │
│    - useCallback for event handlers                     │
│    - React.memo for pure components                     │
└─────────────────────────────────────────────────────────┘
```

### Backend Optimizations

```
┌─────────────────────────────────────────────────────────┐
│ Performance Strategy                                    │
│                                                         │
│ 1. Database Indexing                                    │
│    - Index on shopifyOrderId (UNIQUE)                   │
│    - Index on status (filtering)                        │
│    - Index on updatedAt (sorting)                       │
│                                                         │
│ 2. Queue Processing                                     │
│    - Async job processing                               │
│    - Prevents blocking main thread                      │
│    - Retry logic for failures                           │
│                                                         │
│ 3. Connection Pooling                                   │
│    - PostgreSQL connection pool                         │
│    - Redis connection pool                              │
│    - Reuse connections                                  │
│                                                         │
│ 4. Caching (Optional)                                   │
│    - Redis cache for frequent queries                   │
│    - TTL-based invalidation                             │
│    - Cache-aside pattern                                │
│                                                         │
│ 5. Pagination (Future)                                  │
│    - Limit query results                                │
│    - Cursor-based pagination                            │
│    - Reduce memory usage                                │
└─────────────────────────────────────────────────────────┘
```

---

## 🔄 Deployment Architecture

### Production Setup

```
┌─────────────────────────────────────────────────────────────┐
│                      LOAD BALANCER                          │
│                      (nginx/AWS ALB)                        │
└────────────┬────────────────────────────┬───────────────────┘
             │                            │
             ▼                            ▼
┌────────────────────────┐   ┌────────────────────────┐
│  Frontend Instance 1   │   │  Frontend Instance 2   │
│  (Next.js)             │   │  (Next.js)             │
│  Port: 3000            │   │  Port: 3000            │
└────────────┬───────────┘   └────────────┬───────────┘
             │                            │
             │ HTTP/WebSocket             │
             ▼                            ▼
┌─────────────────────────────────────────────────────────────┐
│                      LOAD BALANCER                          │
│                      (nginx/AWS ALB)                        │
└────────────┬────────────────────────────┬───────────────────┘
             │                            │
             ▼                            ▼
┌────────────────────────┐   ┌────────────────────────┐
│  Backend Instance 1    │   │  Backend Instance 2    │
│  (NestJS)              │   │  (NestJS)              │
│  Port: 9000            │   │  Port: 9000            │
└────────────┬───────────┘   └────────────┬───────────┘
             │                            │
             │                            │
             ▼                            ▼
┌─────────────────────────────────────────────────────────────┐
│                      DATA LAYER                             │
│                                                             │
│  ┌──────────────────┐  ┌──────────────────┐               │
│  │  PostgreSQL      │  │  Redis           │               │
│  │  (RDS/Managed)   │  │  (ElastiCache)   │               │
│  └──────────────────┘  └──────────────────┘               │
└─────────────────────────────────────────────────────────────┘
```

---

**Last Updated**: 2026-01-17
**Version**: 1.0.0
